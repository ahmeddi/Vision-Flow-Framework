# Core ML frameworks
torch>=2.0.0,<2.3.0
torchvision>=0.15.0,<0.18.0
ultralytics>=8.0.0,<8.4.0

# Computer vision
opencv-python>=4.8.0,<5.0.0
Pillow>=9.5.0,<12.0.0
albumentations>=1.3.0,<2.0.0

# Data processing
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0  # Pin to 1.x for compatibility
scipy>=1.11.0,<2.0.0
scikit-learn>=1.3.0,<2.0.0

# Visualization
matplotlib>=3.7.0,<4.0.0
seaborn>=0.12.0,<1.0.0
plotly>=5.15.0,<6.0.0

# Utilities
tqdm>=4.65.0
pyyaml>=6.0
requests>=2.31.0
psutil>=5.9.0
# pynvml>=11.5.0  # Optional: For GPU monitoring

# Model export and optimization
onnx>=1.14.0,<2.0.0
onnxruntime>=1.15.0,<2.0.0

# Enhanced model architectures (optional, may cause conflicts)
super-gradients>=3.1.0,<4.0.0  # YOLO-NAS (S, M, L) - WORKING ✓ (pip shows warning but works fine)
yolox>=0.3.0           # YOLOX models (nano, s, m, l, x) - WORKING ✓
timm>=0.9.0,<2.0.0     # EfficientDet backbones (D0-D7) - WORKING ✓
transformers>=4.30.0,<5.0.0   # DETR and RT-DETR models - WORKING ✓

# YOLOv7 support (clone repo separately, or use via ultralytics in v8.2+)
# git+https://github.com/WongKinYiu/yolov7.git
# Note: Ultralytics now includes YOLOv7 support in newer versions

# PP-YOLOE support (PaddleDetection - optional, large install)
# paddlepaddle>=2.5.0    # PaddlePaddle framework (CPU version)
# paddledet>=2.6.0       # PaddleDetection for PP-YOLOE
# Note: For GPU support, use: paddlepaddle-gpu>=2.5.0 instead of paddlepaddle
# Warning: PaddlePaddle is a large package (~500MB)
# If needed, install separately: pip install paddlepaddle paddledet

# Statistical analysis (optional)
statsmodels>=0.14.0  # WORKING ✓
pingouin>=0.5.0  # WORKING ✓

# Experiment tracking (optional)
wandb>=0.15.0  # WORKING ✓

# Additional dependencies for specialized datasets (optional)
h5py>=3.8.0  # For HDF5 data files - WORKING ✓
# rasterio>=1.3.0       # For multispectral TIFF processing (requires GDAL)
# spectral>=0.23.0      # For hyperspectral analysis

# ============================================
# Installation Instructions:
# ============================================
# For minimal installation (core functionality only):
#   pip install -r requirements-minimal.txt
#
# For full installation with all working packages:
#   pip install -r requirements.txt
#
# Note: pip may show a warning about super-gradients and numpy version,
# but super-gradients works fine with numpy 1.26.4. You can safely ignore it.
#
# ============================================
# Supported Architectures:
# ============================================
# ✅ YOLOv8 (n, s, m, l, x) - via ultralytics package
# ✅ YOLOv11 (n, s, m, l, x) - via ultralytics package  
# ✅ YOLO-NAS (S, M, L) - via super-gradients package
# ✅ YOLOX (nano, tiny, s, m, l, x) - via yolox package
# ✅ YOLOv7 - via ultralytics (v8.2+) or separate repo
# ⚠️ PP-YOLOE (S, M, L, X) - optional, install separately if needed
# ✅ EfficientDet (D0-D7) - backbones via timm package
# ✅ DETR / RT-DETR - via transformers package
#
# ============================================
# Verified Working Versions (Python 3.9):
# ============================================
# torch: 2.2.2
# ultralytics: 8.3.221 (YOLOv8, YOLOv11, YOLOv7, RT-DETR)
# numpy: 1.26.4
# onnx: 1.15.0
# onnxruntime: 1.15.0
# super-gradients: 3.7.1 (YOLO-NAS, works with numpy 1.26.4 despite pip warning)
# yolox: 0.3.0 (YOLOX family)
# timm: 1.0.21 (EfficientDet backbones)
# transformers: 4.57.1 (DETR, RT-DETR)
# wandb: 0.22.2
# statsmodels: 0.14.5
# h5py: 3.14.0
